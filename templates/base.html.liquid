<!DOCTYPE HTML5>
<html>
    <head>
        <title>{{ title }}</title>
        <meta charset="{{ charset }}">
        <link rel="stylesheet" href="/styles.css">
        <base target="_blank">
    </head>
    <body>
        {{ body }}
    </body>
</html>
